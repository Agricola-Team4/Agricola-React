import React from 'react';
import CardSlot from './CardSlot';
import Card from './Card';


export default function CardSlotBoard({img,col,row}) {
    console.log(img);

    return (
        <>
        <div class="bg-major">
        </div>
        </>
    );
}

